/**
 * Fill these in from your Supabase project (Settings, API) to turn on progress sync. The anon
 * key is meant to be public; the SQL in supabase/setup.sql keeps it from listing other records.
 * Leave the placeholders and the app simply saves on this device only.
 */
export const SYNC_CONFIG = {
  url: "https://YOUR-PROJECT.supabase.co",
  anonKey: "",
};
